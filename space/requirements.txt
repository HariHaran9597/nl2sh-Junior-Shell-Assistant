# Keep the Space runtime on a mutually compatible Gradio/Hub pair. Gradio
# 4.44 imports HfFolder, which was removed from huggingface_hub 1.x.
gradio==4.44.0
# Use the published CPU wheel so Hugging Face Spaces does not compile llama.cpp
# from source and hit the build timeout.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python==0.3.35
huggingface_hub==0.25.0
# Gradio 4.44 expects the pre-Starlette-0.40 TemplateResponse API.
fastapi==0.112.2
starlette==0.38.6
jinja2==3.1.4
anyio==4.4.0
uvicorn==0.30.6
pydantic==2.8.2
